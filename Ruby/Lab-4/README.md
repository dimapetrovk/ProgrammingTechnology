#Ruby xml parser with xsd validator

##Required:

 - Ruby version 2.1+
 - sax-machine (sudo gem install sax-machine)
 - rspec (sudo gem install rspec)
 - happymapper (sudo gem install happymapper)
 - nokogiri (sudo gem install nokogiri)